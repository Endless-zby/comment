# 携程酒店评价监控系统 - 详细操作说明

## 目录

1. [系统安装与配置](#1-系统安装与配置)
2. [酒店管理](#2-酒店管理)
3. [评价拉取配置](#3-评价拉取配置)
4. [评价数据查看与筛选](#4-评价数据查看与筛选)
5. [数据导出](#5-数据导出)
6. [仪表盘与统计分析](#6-仪表盘与统计分析)
7. [定时任务管理](#7-定时任务管理)
8. [常见问题与解决方案](#8-常见问题与解决方案)

---

## 1. 系统安装与配置

### 1.1 环境准备

**系统要求：**
- Node.js 18.0 或更高版本
- npm 或 pnpm 包管理器
- 现代浏览器（Chrome、Edge、Firefox等）

**检查 Node.js 版本：**
```bash
node -v
# 应显示 v18.x.x 或更高
```

### 1.2 安装步骤

**步骤一：获取项目代码**
```bash
git clone <repository-url>
cd comment
```

**步骤二：安装依赖**
```bash
npm install
```

如果安装过程中出现网络问题，可以切换国内镜像：
```bash
npm config set registry https://registry.npmmirror.com
npm install
```

**步骤三：配置环境变量**

创建 `.env` 文件：
```bash
cp .env.example .env
```

编辑 `.env` 文件内容：
```env
DATABASE_URL="file:./dev.db"
```

**步骤四：初始化数据库**
```bash
# 创建数据库表结构
npm run db:push

# 生成 Prisma 客户端
npm run db:generate
```

**步骤五：启动系统**
```bash
npm run dev
```

启动成功后，终端会显示：
```
✓ Ready in http://localhost:3000
```

### 1.3 首次访问

打开浏览器，访问 http://localhost:3000

系统会自动跳转到仪表盘页面，此时由于没有数据，页面会显示空状态。

---

## 2. 酒店管理

### 2.1 添加酒店

**操作路径：** 左侧导航栏 → 酒店管理

**添加步骤：**

1. 点击页面右上角「添加酒店」按钮
2. 在弹出的对话框中填写信息：
   - **酒店名称**：必填，用于系统内标识
   - **携程酒店ID**：可选，用于携程平台数据拉取
   - **飞猪酒店ID**：可选，用于飞猪平台数据拉取
   - **城市**：可选，用于分类管理

3. 点击「保存」完成添加

**获取酒店ID的方法：**

**携程酒店ID：**
1. 打开携程官网 https://hotels.ctrip.com
2. 搜索目标酒店并进入详情页
3. 从URL中提取酒店ID
   - URL格式：`https://hotels.ctrip.com/hotel/酒店ID.html`
   - 例如：`https://hotels.ctrip.com/hotel/128045084.html` → ID为 `128045084`

**飞猪酒店ID：**
1. 打开飞猪旅行官网 https://hotel.fliggy.com
2. 搜索目标酒店并进入详情页
3. 从URL或页面元素中提取酒店ID

### 2.2 编辑酒店

1. 在酒店列表中找到目标酒店
2. 点击右侧「编辑」按钮
3. 修改需要更新的信息
4. 点击「保存」完成修改

### 2.3 删除酒店

1. 在酒店列表中找到目标酒店
2. 点击右侧「删除」按钮
3. 在确认对话框中点击「确认删除」

**注意：** 删除酒店会同时删除该酒店的所有评价数据和拉取配置，请谨慎操作。

### 2.4 酒店状态管理

每个酒店有「启用/禁用」状态：
- **启用状态**：酒店参与定时拉取任务
- **禁用状态**：酒店不参与定时拉取，但数据保留

切换状态：点击酒店卡片上的开关按钮

---

## 3. 评价拉取配置

### 3.1 创建拉取配置

**操作路径：** 左侧导航栏 → 配置管理

**配置参数说明：**

| 参数 | 说明 | 默认值 | 推荐值 |
|------|------|--------|--------|
| 拉取间隔(小时) | 自动拉取的时间间隔 | 24 | 6-24 |
| 每页数量 | 每次拉取的评价条数 | 20 | 50 |
| 拉取模式 | 数据拉取策略 | incremental | incremental |
| 启用状态 | 是否参与定时任务 | 启用 | 启用 |

**拉取模式说明：**

- **incremental（增量模式）**：每次只拉取最新一页数据，遇到已存在的评价停止
  - 适合日常维护，效率高
  - 首次拉取时会自动拉取多页（默认5页）

- **full（全量模式）**：每次拉取所有可获取的评价
  - 适合数据初始化或完整重建
  - 拉取时间较长

### 3.2 创建配置步骤

1. 点击「添加配置」按钮
2. 选择关联的酒店
3. 设置拉取参数
4. 点击「保存」

### 3.3 手动触发拉取

**方式一：配置管理页面**
1. 在配置列表中找到目标配置
2. 点击「立即拉取」按钮
3. 系统会启动拉取任务

**方式二：酒店管理页面**
1. 在酒店列表中找到目标酒店
2. 点击「拉取评价」按钮
3. 选择拉取平台（携程/飞猪）

### 3.4 查看拉取日志

每个配置下方显示最近的拉取记录：
- **状态**：成功/失败
- **新增数量**：本次拉取新增的评价数
- **拉取页数**：本次拉取的页面数
- **时间**：拉取完成时间
- **错误信息**：失败时显示错误详情

---

## 4. 评价数据查看与筛选

### 4.1 进入评价列表

**操作路径：** 左侧导航栏 → 评价列表

### 4.2 筛选条件

页面顶部提供多种筛选条件：

**酒店筛选：**
- 下拉选择已添加的酒店
- 必须选择酒店才能查看评价

**平台筛选：**
- 全部平台：显示携程和飞猪的所有评价
- 携程：仅显示携程平台评价
- 飞猪：仅显示飞猪平台评价

**评分筛选：**
- 全部评分：显示所有评分的评价
- 好评（4-5星）：仅显示评分≥4的评价
- 中评（3星）：仅显示评分在3-4之间的评价
- 差评（1-2星）：仅显示评分<3的评价

**关键词搜索：**
- 输入关键词后点击「搜索」按钮
- 搜索评价内容中包含关键词的评价

### 4.3 评价详情展示

每条评价显示以下信息：

**基本信息：**
- 评分星级可视化
- 评价类型标签（好评/中评/差评）
- 平台来源标签（携程/飞猪）
- 房型、入住日期

**评价内容：**
- 完整评价文本
- 支持图片展示（点击图片可放大查看）

**酒店回复：**
- 如果酒店有回复，会在评价下方显示

**元信息：**
- 评价者昵称
- 评价日期
- 评价ID

### 4.4 分页浏览

- 每页显示20条评价
- 底部显示分页导航
- 显示总条数和当前页码
- 点击「上一页」「下一页」切换

### 4.5 刷新数据

点击「刷新」按钮重新加载当前筛选条件的数据

---

## 5. 数据导出

### 5.1 导出功能入口

**位置：** 评价列表页面 → 筛选条件区域 → 「导出Excel」按钮

### 5.2 导出操作步骤

1. 设置筛选条件（酒店、平台、评分、关键词）
2. 点击「导出Excel」按钮
3. 系统自动生成Excel文件并下载

### 5.3 导出文件内容

Excel文件包含以下字段：

| 列名 | 说明 |
|------|------|
| 序号 | 自动编号 |
| 酒店名称 | 评价所属酒店 |
| 平台 | 携程/飞猪 |
| 评分 | 数值评分 |
| 评价类型 | 好评/中评/差评 |
| 房型 | 房间类型名称 |
| 入住日期 | 用户入住时间 |
| 评价日期 | 发布评价时间 |
| 评价者 | 用户昵称 |
| 评价内容 | 评价正文 |
| 是否有图片 | 是/否 |
| 图片链接 | 多个链接换行分隔 |
| 酒店回复 | 酒店回复内容 |
| 评价ID | 唯一标识 |

### 5.4 文件命名规则

导出文件名格式：`评价导出_酒店名_平台_评分类型_日期.xlsx`

示例：
- `评价导出_上海世博酒店_携程_好评_2026-04-29.xlsx`
- `评价导出_全部酒店_全部平台_全部评分_2026-04-29.xlsx`

### 5.5 导出注意事项

- 导出包含**所有匹配的数据**，不受分页限制
- 大量数据导出可能需要等待几秒
- 图片链接可直接复制到浏览器查看

---

## 6. 仪表盘与统计分析

### 6.1 仪表盘概览

**操作路径：** 左侧导航栏 → 仪表盘

**显示内容：**

- **酒店总数**：已添加的酒店数量
- **评价总数**：累计拉取的评价数量
- **平均评分**：所有评价的平均评分
- **今日新增**：当天新增的评价数量

### 6.2 评分分布图表

- 饼图展示好评、中评、差评的比例
- 鼠标悬停显示具体数量和百分比

### 6.3 评分趋势图

- 投线图展示近期评分变化趋势
- 横轴为日期，纵轴为平均评分

### 6.4 统计分析页面

**操作路径：** 左侧导航栏 → 统计分析

**功能模块：**

- **周报汇总**：按周统计评价数量、评分分布
- **平台对比**：携程与飞猪数据对比分析
- **酒店排名**：按评分、评价数量排序

---

## 7. 定时任务管理

### 7.1 定时任务原理

系统使用 node-cron 实现定时任务：
- 每小时检查是否有需要拉取的配置
- 根据配置的「拉取间隔」判断是否执行
- 自动记录拉取日志

### 7.2 启用/禁用定时任务

**全局控制：** 系统设置页面 → 定时任务开关

**单酒店控制：** 配置管理页面 → 单个配置的启用状态

### 7.3 定时任务状态查看

在配置管理页面可以查看：
- 最后拉取时间
- 下次预计拉取时间
- 累计拉取数量

---

## 8. 常见问题与解决方案

### 8.1 拉取失败

**问题现象：** 拉取日志显示失败状态

**可能原因与解决方案：**

| 原因 | 解决方案 |
|------|----------|
| 酒店ID错误 | 检查并修正酒店ID |
| 网络问题 | 检查网络连接，稍后重试 |
| 平台反爬 | 系统会自动重试，多次失败需检查配置 |
| 浏览器实例问题 | 重启系统服务 |

### 8.2 数据不完整

**问题现象：** 拉取的评价数量少于预期

**解决方案：**
1. 检查拉取模式设置
2. 尝试手动触发多次拉取
3. 查看拉取日志了解停止原因

### 8.3 页面加载缓慢

**问题现象：** 评价列表页面加载时间长

**解决方案：**
1. 使用筛选条件减少数据量
2. 检查数据库文件大小
3. 考虑清理历史数据

### 8.4 导出功能异常

**问题现象：** 导出按钮无响应或报错

**解决方案：**
1. 确认已安装 xlsx 依赖：`npm install xlsx`
2. 检查浏览器控制台错误信息
3. 尝试减少导出数据量

### 8.5 数据库相关

**查看数据库：**
```bash
npm run db:studio
```
打开 Prisma Studio 可视化管理数据库

**重置数据库：**
```bash
# 删除数据库文件
rm prisma/dev.db

# 重新创建
npm run db:push
```

### 8.6 开发调试

**查看系统日志：**
- 拉取日志存储在数据库 FetchLog 表
- 服务日志输出到终端控制台

**调试模式：**
```bash
# 启动开发模式（带详细日志）
npm run dev
```

---

## 附录：快捷键与操作技巧

| 操作 | 说明 |
|------|------|
| 点击图片 | 在新窗口查看大图 |
| 刷新按钮 | 重新加载当前数据 |
| 导出Excel | 导出当前筛选结果 |
| 状态开关 | 快速启用/禁用 |

---

## 技术支持

如遇到其他问题，请查看：
- 项目 README.md 文档
- GitHub Issues 页面
- 项目技术规则文档 `.trae/rules/product-rule.md`